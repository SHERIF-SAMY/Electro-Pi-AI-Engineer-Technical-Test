version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ollama_data:/root/.ollama
    restart: unless-stopped
    env_file:
      - .env

volumes:
  ollama_data:
